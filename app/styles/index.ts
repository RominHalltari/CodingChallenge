import * as defaultColorStyles from "app/styles/default/colors";

export const light = defaultColorStyles.light;
export const xLight = defaultColorStyles.xLight;
export const xxLight = defaultColorStyles.xxLight;
export const xxxLight = defaultColorStyles.xxxLight;
